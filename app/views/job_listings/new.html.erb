<h1>New job listing <%= 'for ' + CompanyProfile.find(params[:company_profile]).company_name if params[:company_profile] %></h1>

<%= render 'form', job_listing: @job_listing %>

<%= link_to 'Back', job_listings_path %>
